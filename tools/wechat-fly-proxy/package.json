{
  "name": "wechat-fly-proxy",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Standalone passthrough proxy to api.weixin.qq.com, deployed on Fly.io for a stable egress IP.",
  "engines": { "node": ">=20" },
  "scripts": {
    "start": "node server.mjs"
  }
}
