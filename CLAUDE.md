# 写给协作者（含 AI）的约定

本仓库是 Max（Ma Wei）的博客 maweis.com 与公众号「几墨春秋」的内容库。以下是长期约定，写文章、配图、发布时请默认遵守。

## 一、Max 的写作口吻（默认预设）

参照范文：`_posts/2026-07-17-reading-aloud.md`（《出声》）、`_posts/2026-07-25-higashino-keigo.md`（《送别东野圭吾》）。亦舒式的冷与省，是这条调子的近亲。

要点：

- **短句**。一句一个意思，句子之间敢留白、敢分段。
- **克制**。情绪压住，不煽情、不喊口号。深情藏在事实和一两个具体画面底下，让它自己渗出来，不直接说。
- **点到为止**。信任读者，不做"科普老师"式的层层展开，不堆术语。数学、哲学这些"硬"东西只轻轻点一下当底色，不铺成讲义。
- **开头**从一个具体的场景、细节或动作切入，不从宏大概括切入。
- **结尾**落在一句短的、克制的断言或顿悟上（金句收尾），不总结、不升华腔。
- **第一人称"我"**，像跟一个信得过的朋友说话，冷静里有温度。

硬性禁止：

- 不用**破折号**（——）。用逗号、句号、冒号代替。
- 不用这些 AI 腔口头禅："说句老实话""换句话说""值得注意的是""总而言之""不难发现""令人不禁"等。
- 不堆砌形容词、不硬凑排比对仗。

## 二、内容与事实

- **事实先核实，绝不胡说**。人物生死、时事、数据等重要事实，先查证再写；不确定的明确标注，纯观点标"一家之言"。
- 需要论据时，**文末列出处**。
- 诚实优先：不夸大、不臆造引用、不给没读过的东西挂名。

## 三、标题

- 走"认知颠覆 / 共鸣"路子，但**不庸俗、不标题党**，要配得上正文的分量。
- 避免把现象说成"人人如此"的铁律，承认个体差异（用"默认倾向""容易"这类措辞）。

## 四、配图

- 用 Floniks 生成（模型 `openai_gpt_image_2_t2i`，参数 `{"quality":"high","size":"1536x1024"}`），失败自动退 credit。
- 主视觉：深蓝紫 + 暖金的系列风格；悼念等特殊题材可改用更克制的色调。
- **画面内不出现任何文字/数字**（prompt 里明确 no text）。
- 下载后转成 JPEG，存 `assets/img/posts/<slug>/cover.jpg`；front matter 里 `image.path` 指向它，`alt` 结尾注明"（AI 生成）"。

## 五、发布流程

- 文章放 `_posts/YYYY-MM-DD-<slug>.md`。**日期用真实的当前日期（北京时间）**。permalink 由文件名 slug 决定，与日期前缀无关，改日期不会改链接。
- front matter 需含 `title / date / author: Max (Ma Wei) / categories / tags / description / image / wechat_source_url`。
- 默认同时发博客与公众号；**只发博客**时加 `wechat: false`。
- 合并到 `master` 会触发 GitHub Actions：Pages 部署 + 公众号草稿生成（`_posts/*.md`、`_wechat/*.md`，除非 `wechat: false`）。
- **先给 Max 过目、批准后再发**。

## 六、现有栏目（categories）

- `算得出 ≠ 证得了`：数学与证明的边界（黎曼、孪生素数、费马、王虹/挂谷）。
- `生活是道数学题`：生活里的数学（爱情/最优停止、报志愿/稳定匹配）。
- `AI 没那么玄`：把 AI 概念讲人话（MIT 课程转写）。
- `大问题`：文明尺度的思想长文（AI 与分工、认知与决策）。
- `万物皆数`：藏在自然里的数学。
- `随笔`：文学性随笔（《出声》《送别东野圭吾》）。
